version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - run:
          name: packages
          command: sudo apt-get -y update; sudo apt-get -y install git build-essential
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
